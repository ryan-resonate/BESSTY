[workspace]
resolver = "2"
members = ["crates/iso9613-core", "crates/iso9613-wasm"]

# Shared release profile: LTO + single codegen unit does most of what wasm-opt
# would (see crates/iso9613-wasm/Cargo.toml for why wasm-opt itself is off).
[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1

[profile.bench]
inherits = "release"
